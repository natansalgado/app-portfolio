import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        paddingTop: 10,
        paddingBottom: 8,
        width: '100%',
        marginTop: 20,
        borderRadius: 5,
        borderWidth: 2,
        textTransform: 'uppercase',
        fontWeight: 'bold',
        fontSize: 16,
        textAlign: 'center',
        elevation: 4,
        color: '#FFF'
    }
});