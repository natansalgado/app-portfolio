import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        alignItems: 'center',
        top: 20,
        right: 10,
        padding: 5
    },
    bulb: {
        marginTop: 10,
        fontSize: 30,
    },
    text: {
        textTransform: 'uppercase'
    }
});