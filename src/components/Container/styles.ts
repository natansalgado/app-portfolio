import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginTop: 20,
        borderWidth: 2,
        borderRadius: 5,
        paddingHorizontal: 10,
        paddingBottom: 15,
        width: '80%',
        elevation: 4
    }
});